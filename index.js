autos= require("./autos");
let concesionaria = {
autos: autos,
buscarAuto: function (patente) {
            if (autos.indexOf (unNumero)) {
                return concesionaria.autos;
            } else {
                return null;
            };
        }
}

console.log(concesionaria.autos);